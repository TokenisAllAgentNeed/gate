# cashu-gate — Cashu ecash payment gateway
# Copy to .dev.vars for local development

# Upstream LLM API keys (at least one required)
OPENAI_API_KEY=sk-your-openai-key
OPENROUTER_API_KEY=sk-or-your-openrouter-key

# Trusted Cashu mints (comma-separated)
TRUSTED_MINTS=https://mint.token2chat.com

# Mint URL for swap/redeem
MINT_URL=https://mint.token2chat.com

# Melt threshold in sats (accumulate ecash, then melt to Lightning)
MELT_THRESHOLD_SATS=10000
